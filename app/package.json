{
  "name": "bmdc-receptionist-console",
  "version": "1.0.0",
  "private": true,
  "description": "Owner's console for a BMDC Receptionist server.",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
